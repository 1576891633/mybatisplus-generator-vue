package ${pkgPath};

import ${formPath};
import ${modelPath};

import java.util.List;

public interface ${clzName}Service {

    /**
     * 分页查询
     * @return
     */
    PageInfo findByPage(int page, int size,String sort,String dir,${formName} ${clzNameLC});

    PageInfo findByPage(int page, int size,${formName} ${clzNameLC});

    PageInfo findByPage(int page, int size,String sort,String dir);

    PageInfo findByPage(int page, int size,String sort);

    PageInfo findByPage(int page, int size);

    /**
     * 列表查询
     * @return
     */
    List<${clzName}> findAll(${formName} ${clzNameLC});

	/**
	 * 单个查询
	 */
	${clzName} getById(${parserKeyType} ${parserKey});

	/**
	 * 新增
	 */
	int save(${clzName} ${clzNameLC});

	 /**
	 * 修改
	 */
	int update(${clzName} ${clzNameLC});

	 /**
	 * 删除
	 */
	int deleteByIds(String[] ids);
}
